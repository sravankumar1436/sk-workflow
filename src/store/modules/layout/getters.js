export const getters = {
    drawer: state => {
        return state.drawer;
    },

    toolbar: state => {
        return state.toolbar;
    },

    resizeDrawer: state => {
        return state.resizeDrawer;
    }
}


export default getters;
